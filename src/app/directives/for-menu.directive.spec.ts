import { ForMenuDirective } from './for-menu.directive';

describe('ForMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ForMenuDirective();
    expect(directive).toBeTruthy();
  });
});
